// components/cart-item/index.js
import {Cart} from "../../models/cart";

const cart = new Cart()

Component({
    /**
     * 组件的属性列表
     */
    properties: {
    },

    /**
     * 组件的初始数据
     */
    data: {
    },

    observers: {
    },

    /**
     * 组件的方法列表
     */
    methods: {
    }
})
